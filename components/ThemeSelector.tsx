import React from 'react';

// This component is not currently used but the file is kept for potential future use.
// It was previously corrupted and has been cleaned.
export default function ThemeSelector() {
  return null;
}
